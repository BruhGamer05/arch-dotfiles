# arch-dotfiles

My dotfiles for Arch Linux + Hyprland, managed with GNU Stow.

## Usage

    git clone https://github.com/BruhGamer05/arch-dotfiles.git ~/arch-dotfiles
    cd ~/arch-dotfiles
    stow hypr

Each top-level folder here is a stow "package" mirroring the structure that goes inside `$HOME`.
